kafka:
  bootstrapAddress: localhost:9092

application:
  name: AuctionService
  id: 0

server:
  port: 9200


spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/AuctionServiceDB?createDatabaseIfNotExist=true
    username: root
    password: root
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update